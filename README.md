# CYBERTAG Showcase

Многостраничный сайт CYBERTAG с серверной регистрацией и входом через Microsoft SQL Server 2022 Express. Интерфейс выполнен в самостоятельной чёрно-красной стилистике с компоновкой современной торговой витрины.

## Реализовано

- каталог оборудования, комплекты, софт, акции и контакты;
- общий хедер с каталогом, поиском и быстрым доступом к аккаунту и корзине;
- промовитрина, категории и карточки комплектов на главной странице;
- отдельная корзина со счётчиком, удалением и очисткой;
- регистрация с сохранением пользователя в таблицах `Customers` и `Users`;
- bcrypt-хэширование паролей;
- вход по логину или email;
- серверные сессии в таблице `Sessions` и защищённая `HttpOnly` cookie;
- выход с удалением сессии из SQL Server;
- автоматический перенос состава корзины в форму контактов;
- адаптивная версия для компьютеров и мобильных устройств.

## Требования

- Node.js 20 или новее;
- Microsoft SQL Server 2022 Express, экземпляр `SQLEXPRESS`;
- ODBC Driver 17 for SQL Server.

## Запуск

```powershell
npm install
npm run db:init
npm start
```

Сайт и API будут доступны по адресу `http://127.0.0.1:3000/`.

Параметры подключения находятся в `.env.example`. Для локального запуска используется Windows Authentication. Файл `.env` не добавляется в Git.

## Проверка

```powershell
npm test
```

Интеграционный тест создаёт отдельную временную SQL-базу, проверяет регистрацию, уникальность логина/email, сессию, выход и повторный вход, затем удаляет тестовую базу.

## Демо-вход

- Пользователь: `manager`
- Пароль: `cybertag2026`

Демонстрационный пользователь создаётся командой `npm run db:init`. На GitHub Pages эти же данные работают в локальном демонстрационном режиме без передачи пароля на сторонний сервер. В рабочем окружении задайте `SEED_DEMO_USER=0` и создавайте пользователей через регистрацию.

## Публикация

GitHub Pages показывает статическую витрину с корзиной, поиском и демо-входом. Полная регистрация и серверные сессии требуют запуска приложения на Windows-сервере с Node.js, SQL Server и ODBC Driver 17.

- Репозиторий: https://github.com/alextopgg228-cmyk/cybertag-showcase
- Статическая витрина: https://alextopgg228-cmyk.github.io/cybertag-showcase/
- Отчёт: `docs/Otchet_PM04_CYBERTAG_SQL.docx`
