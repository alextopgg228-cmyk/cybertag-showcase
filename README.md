# CYBERTAG Showcase

Многостраничный сайт CYBERTAG с серверной регистрацией и входом через Microsoft SQL Server 2022 Express. Исходное чёрно-красное оформление сохранено.

## Реализовано

- каталог оборудования, комплекты, софт, акции и контакты;
- отдельная корзина со счётчиком, удалением и очисткой;
- регистрация с сохранением пользователя в таблицах `Customers` и `Users`;
- bcrypt-хэширование паролей;
- вход по логину или email;
- серверные сессии в таблице `Sessions` и защищённая `HttpOnly` cookie;
- создание заказов из корзины в таблицах `Orders` и `OrderItems`;
- административная панель со всеми заказами и изменением их статуса;
- добавление и снятие акций с публикации через таблицу `Promotions`;
- выход с удалением сессии из SQL Server;
- автоматический перенос состава корзины в форму контактов;
- адаптивная версия без изменения исходной стилистики.

## Требования

- Node.js 20 или новее;
- Microsoft SQL Server 2022 Express, экземпляр `SQLEXPRESS`;
- ODBC Driver 17 for SQL Server.

## Запуск

```powershell
npm install
npm run db:init
npm start
```

Сайт и API будут доступны по адресу `http://127.0.0.1:3000/`.

Параметры подключения находятся в `.env.example`. Для локального запуска используется Windows Authentication. Файл `.env` не добавляется в Git.

## Проверка

```powershell
npm test
```

Интеграционный тест создаёт отдельную временную SQL-базу, проверяет регистрацию, уникальность логина/email, сессию, выход и повторный вход, затем удаляет тестовую базу.

## Демо-вход

- Пользователь: `manager`
- Пароль: `cybertag2026`

Демонстрационный пользователь имеет роль `admin` и открывает защищённую страницу `/admin/`. Гости перенаправляются на вход, а пользователи с ролью `customer` — в личный кабинет. Администратор создаётся командой `npm run db:init`.

## Публикация

GitHub Pages показывает статическую витрину и включает автономный демо-режим входа/регистрации в браузере, чтобы публичная ссылка работала без включённого компьютера. Демо-данные на GitHub Pages хранятся только в браузере посетителя.

Полная версия с настоящей серверной регистрацией и SQL Server запускается через Node.js. Для постоянной серверной версии без локального компьютера приложение нужно разместить на отдельном backend-хостинге с доступом к облачной SQL-базе.

- Репозиторий: https://github.com/alextopgg228-cmyk/cybertag-showcase
- Публичная демо-версия: https://alextopgg228-cmyk.github.io/cybertag-showcase/
- Отчёт: `docs/Otchet_PM04_CYBERTAG_SQL.docx`
